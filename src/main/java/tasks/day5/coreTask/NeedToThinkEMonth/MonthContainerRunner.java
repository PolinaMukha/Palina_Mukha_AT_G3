package main.java.tasks.day5.coreTask.NeedToThinkEMonth;

public class MonthContainerRunner {
/*    public static void main(String[] args) {
        public void printStringMonth (String month){
            if (month.equals(MonthContainer.JANUARY.getMonth())) {
                System.out.println(MonthContainer.FEBRUARY.getMonth());
            } else if (month.equals(MonthContainer.FEBRUARY.getMonth())) {
                System.out.println(MonthContainer.MARCH.getMonth());
            } else if (month.equals(MonthContainer.MARCH.getMonth())) {
                System.out.println(MonthContainer.APRIL.getMonth());
            } else if (month.equals(MonthContainer.APRIL.getMonth())) {
                System.out.println(MonthContainer.MAY.getMonth());
            } else if (month.equals(MonthContainer.MAY.getMonth())) {
                System.out.println(MonthContainer.JUNE.getMonth());
            } else if (month.equals(MonthContainer.JUNE.getMonth())) {
                System.out.println(MonthContainer.JULY.getMonth());
            } else if (month.equals(MonthContainer.JULY.getMonth())) {
                System.out.println(MonthContainer.AUGUST.getMonth());
            } else if (month.equals(MonthContainer.AUGUST.getMonth())) {
                System.out.println(MonthContainer.SEPTEMBER.getMonth());
            } else if (month.equals(MonthContainer.SEPTEMBER.getMonth())) {
                System.out.println(MonthContainer.OCTOBER.getMonth());
            } else if (month.equals(MonthContainer.OCTOBER.getMonth())) {
                System.out.println(MonthContainer.NOVEMBER.getMonth());
            } else if (month.equals(MonthContainer.NOVEMBER.getMonth())) {
                System.out.println(MonthContainer.DECEMBER.getMonth());
            } else if (month.equals(MonthContainer.DECEMBER.getMonth())) {
                System.out.println(MonthContainer.JANUARY.getMonth());
            }
        }
    }*/
}
